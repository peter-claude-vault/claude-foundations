.DS_Store
/results/
/dogfood-history/
.image-digest
*.log
# Three-step gate audit log written at runtime by lib/three-step-gate.sh
# (default path: onboarding/auto-author-log.jsonl) during fixture runs.
/onboarding/auto-author-log.jsonl
/.self-verify/
/.e2e/
/.e2e-scenario.sh
# Per-task evidence is plan-tree scope (~/.claude-plans/71-*/<sub>/state/),
# not foundation-repo scope. SP10 T-13 (audit MEDIUM #1) — 2026-05-03.
/state/
# SP13 T-4 (Stage 2 INFER) cluster output is a runtime artifact, not source.
# Default --out for skills/infer-vault-structure/cluster.sh writes here.
/onboarding/seed-content/state/
__pycache__/
# Documentation overhaul scratch directory
/_doc-overhaul/
# Obsidian vault config (local UI state — workspace layout, theme, file
# order, etc.). Created automatically when foundation-repo is opened as
# an Obsidian vault for review. Not source; never shipped via install.sh.
/.obsidian/
