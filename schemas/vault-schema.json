{
  "schema_version": "2.0.0",
  "_dissolved": true,
  "_dissolved_marker": {
    "status": "DISSOLVED — DO NOT AUTHOR HERE",
    "frozen_at": "2026-05-14",
    "dissolution_sp_task": "SP13 T-4",
    "rationale": "This kitchen-sink schema dissolved into single-concern governance pillars per feedback_no_kitchen_sink_pillars. Content remains intact below for backward-compatibility during the bridge period; consumers progressively migrate to the named successors below. File deletion is scheduled atomically with the last consumer migration (SP13 T-9 or T-10) per OQ-13-4 'migrate-then-drop in same commit' + 'drop-then-fail-loudly disallowed.'",
    "canonical_successors": {
      "types (21 type entries)": "governance/frontmatter-rules.json#types",
      "tiers (strict/standard/minimal)": "governance/frontmatter-rules.json#tier_compliance",
      "_archetype_conditional_fields": "governance/frontmatter-rules.json#archetype_conditional_fields",
      "_archetype_enum (was implicit)": "governance/frontmatter-rules.json#archetype_enum",
      "_packet_only_fields": "governance/frontmatter-rules.json#packet_only_fields",
      "_excluded_fields": "governance/frontmatter-rules.json#excluded_fields",
      "_retired_types": "governance/frontmatter-rules.json#retired_types",
      "_path_rules (renamed path_routing)": "governance/frontmatter-rules.json#path_routing",
      "_tag_prefixes + _tag_prefixes_meta": "governance/tagging-rules.json#taxonomy"
    },
    "consumer_migration_status": {
      "governance_authoring_pillars": "MIGRATED in SP13 T-4 (2026-05-14 commit c4c79ea)",
      "hooks/pre-write-guard.sh": "MIGRATED in SP13 T-3 (2026-05-14 this commit) — reads foundation-master.json bundle exclusively (SCHEMA_FILE direct-read removed; UPDATED_IN_SCHEMA + REQUIRED_FIELDS + CONDITIONAL_FIELDS now read from bundle.types[$key])",
      "hooks/post-write-verify.sh": "MIGRATED in SP13 T-3 (2026-05-14 this commit) — reads foundation-master.json bundle exclusively (python schema extraction now bundle.types; was vault-schema.json top level)",
      "hooks/config/gate-config.json schema_path field": "DISSOLVED in SP13 T-3 (2026-05-14 this commit) — gate-config dissolved with marker; physical retirement T-6",
      "install.sh schema-copy ship list": "PENDING SP13 T-8 — remove vault-schema from ship list",
      "onboarding/bootstrap-schemas.sh _tag_prefixes assertion": "PENDING SP13 T-8 — replace with tagging-rules.json assertion",
      "tests/*": "PENDING SP13 T-9 — update fixtures",
      "docs + research + target-state narratives": "PENDING SP13 T-10 — flip references"
    },
    "policy": "(1) No new authoring against this file — all updates go to the named successors. (2) Content below is FROZEN as of 2026-05-14; reads from this file return historical state. (3) Until the last consumer migrates, this file ships via install.sh and validates via bootstrap-schemas.sh exactly as before — runtime semantics preserved. (4) The governance/_index.json `dissolved_schemas` registry tracks final-deletion gating.",
    "registry_entry": "governance/_index.json#dissolved_schemas.\"schemas/vault-schema.json\""
  },
  "_design_notes": {
    "model": "unified-with-per-archetype-entries",
    "tiers": "Strict | Standard | Minimal",
    "consumer": "pre-write-guard.sh case statement; loaded at runtime",
    "lockstep_pair": "frontmatter-rules.json ↔ Vault Architecture - Frontmatter.md",
    "evolution_protocol": "atomic lockstep — schema + hook + docs updated in one commit",
    "source_dependencies": [
      "../docs/decisions/0001-tiered-compliance.md",
      "../docs/decisions/0002-unified-with-per-archetype-entries.md",
      "../docs/decisions/0003-folder-lineage-as-fields.md",
      "../docs/decisions/0004-system-utility-dimension-exemption.md",
      "../research/vault-construction/frontmatter-design.md",
      "../research/vault-construction/vault-construction-principles.md",
      "../research/vault-construction/enforcement-map-design.md"
    ]
  },

  "tiers": {
    "strict": {
      "description": "System files (scaffold-emitted, /ingest-routed, scraper-aggregated to Inbox/). Hard fail at write-time (R-32 Tier 2 DENY).",
      "validation_behavior": "deny",
      "applies_to": "type entries with tier=strict in this schema",
      "universal_required": ["type", "tags", "updated"],
      "conditional_required": ["status"]
    },
    "standard": {
      "description": "User-authored vault content. Soft warning; librarian flags drift via tag-coverage-audit + frontmatter-coverage-audit capabilities.",
      "validation_behavior": "warn",
      "applies_to": "type entries with tier=standard in this schema",
      "universal_required": ["type", "tags", "updated"],
      "conditional_required": []
    },
    "minimal": {
      "description": "Explicit opt-out files. No validation; flagged 'outside system' by librarian. Used for legacy imports, paste-buffer scratch, archives outside lifecycle.",
      "validation_behavior": "allow",
      "applies_to": "files matching _minimal_tier_exemptions glob patterns OR carrying explicit `tier: minimal` frontmatter directive",
      "universal_required": [],
      "conditional_required": []
    }
  },

  "_archetype_conditional_fields": {
    "_description": "Fields that may appear in any type entry's required/optional list. Per-type entries declare which apply (the 'per-archetype entries' in the unified model). Adopters extend this list via Layer 3 vault-overlay; the 14 entries below are the foundation-repo seed.",
    "fields": [
      "engagement",
      "project",
      "workstream",
      "owner",
      "provides",
      "created",
      "name",
      "attendees",
      "processed",
      "granola_id",
      "target_date",
      "audience",
      "github_repo",
      "parent_folder"
    ]
  },

  "_packet_only_fields": {
    "_description": "Fields valid only on entries with type=packet (system-altitude research context packets at `claude-stem/research/vault-construction/`). Engagement/topic/initiative altitude packets at the user vault use the same shape via the `packet` type entry below.",
    "fields": [
      "altitude",
      "validity_window",
      "source_dependencies",
      "last_reviewed",
      "canonical_url",
      "url_stability"
    ]
  },

  "_excluded_fields": {
    "parent_plan": {
      "reason": "Plan-tree-only field; lives in `plans-schema.json`, not vault schema.",
      "disposition": "Deferred from v1. Re-visit when planning methodology generalizes for adopters.",
      "do_not": "Do NOT add `parent_plan` to any vault-schema type entry. Plan-tree files validate against `plans-schema.json` separately."
    }
  },

  "_retired_types": {
    "engagement": {
      "decision_ref": "../docs/decisions/0003-folder-lineage-as-fields.md",
      "reason": "Type information lives at file level only — folders don't have frontmatter. `engagement` is NOT a canonical TYPE; it is a FIELD slot that propagates folder lineage to files. Folders own structural artifact; field + tag propagate to file level.",
      "replacement": "Use `engagement: <slug>` as FIELD on any file living under `Engagements/<X>/Projects/<Y>/` (see _path_rules.consultant-engagement-project-lineage). For the engagement-level navigation file itself (e.g., `Engagements/<X>/CLAUDE.md`), use type=`navigation`."
    },
    "project": {
      "decision_ref": "../docs/decisions/0003-folder-lineage-as-fields.md",
      "reason": "Same as `engagement` — folders own project lineage; FIELD `project` propagates lineage to files.",
      "replacement": "Use `project: <slug>` as FIELD on any file living under `Engagements/<X>/Projects/<Y>/` (see _path_rules.consultant-engagement-project-lineage). For the project-level overview file itself, use type=`context` or type=`navigation` (per file purpose)."
    }
  },

  "_path_rules": {
    "_description": "Cross-cutting folder-lineage validation rules. Hook contract: when a file is written, evaluate matching path rules; if pattern matches, the file MUST satisfy the requires_fields + requires_tags clauses. Encoded generically so adopter archetypes (researcher's Topics/<X>/Studies/<Y>/; developer's Repos/<X>/Epics/<Y>/) extend the array without schema-shape changes. Foundation-repo ships with the consultant default.",
    "rules": [
      {
        "id": "consultant-engagement-project-lineage",
        "archetype": "consultant",
        "pattern": "Engagements/{engagement_slug}/Projects/{project_slug}/**",
        "requires_fields": [
          { "field": "engagement", "must_equal_ancestor_segment_at_depth": 1 },
          { "field": "project", "must_equal_ancestor_segment_at_depth": 3 }
        ],
        "requires_tags": [
          { "tag_pattern": "#engagement/{engagement_slug}" },
          { "tag_pattern": "#project/{project_slug}" }
        ],
        "tier": "strict",
        "rationale": "Folder-lineage convention: folder is structural artifact; frontmatter fields + tags are file-level workaround that propagates lineage to every file. The R-32 hook contract validates lineage consistency at write-time. See ../docs/decisions/0003-folder-lineage-as-fields.md.",
        "exemptions": [
          "Files at Engagements/<X>/CLAUDE.md (engagement-level navigation; type=navigation entry handles)",
          "Files at Engagements/<X>/_index.md (folder index; type=index entry handles)",
          "Files at Engagements/<X>/Projects/<Y>/CLAUDE.md (project-level navigation; type=navigation entry handles)",
          "Files at Engagements/<X>/Projects/<Y>/_index.md (project folder index; type=index entry handles)"
        ]
      }
    ]
  },

  "_tag_prefixes": [
    "engagement",
    "project",
    "scope",
    "status",
    "initiative",
    "artefact-bd",
    "about-me",
    "log"
  ],

  "_tag_prefixes_meta": {
    "_description": "Eight-dimension faceted taxonomy. Adopters extend via Layer 3 vault-overlay using per-archetype synonym dictionaries. System-utility dimensions (#log/*, #status/*) exempt from 25-tag cap; bound to log-subtype-registry discipline instead. See ../docs/decisions/0004-system-utility-dimension-exemption.md.",
    "system_utility_dimensions": ["log", "status"],
    "user_facing_dimensions": ["engagement", "project", "scope", "initiative", "artefact-bd", "about-me"],
    "cap_25_applies_to": "user_facing_dimensions only"
  },

  "meeting-note": {
    "tier": "strict",
    "required": [
      "type",
      "date",
      "meeting_title",
      "attendees",
      "tags",
      "processed",
      "updated"
    ],
    "optional": [
      "engagement",
      "project",
      "previous_instance",
      "granola_id",
      "granola_ids",
      "granola_url"
    ]
  },

  "daily-note": {
    "tier": "strict",
    "required": [
      "type",
      "date",
      "day",
      "processed",
      "tags"
    ],
    "optional": []
  },

  "inbox-archive": {
    "tier": "strict",
    "required": [
      "type",
      "date",
      "day",
      "sources",
      "created",
      "tags"
    ],
    "optional": []
  },

  "log": {
    "tier": "strict",
    "required": [
      "type",
      "log-type",
      "date",
      "timestamp"
    ],
    "optional": [
      "tags"
    ]
  },

  "people": {
    "tier": "standard",
    "required": [
      "type",
      "name",
      "org",
      "role",
      "engagement",
      "updated",
      "tags"
    ],
    "optional": [
      "email",
      "projects",
      "max_lines"
    ]
  },

  "prd": {
    "tier": "standard",
    "required": [
      "title",
      "engagement",
      "project",
      "type",
      "status",
      "owner",
      "updated",
      "tags"
    ],
    "optional": [
      "workstream",
      "provides",
      "max_lines"
    ]
  },

  "context": {
    "tier": "standard",
    "required": [
      "title",
      "engagement",
      "project",
      "type",
      "status",
      "owner",
      "updated",
      "tags"
    ],
    "optional": [
      "workstream",
      "provides",
      "max_lines"
    ]
  },

  "reference": {
    "tier": "standard",
    "required": [
      "type",
      "updated",
      "tags"
    ],
    "optional": [
      "provides",
      "max_lines"
    ]
  },

  "index": {
    "tier": "strict",
    "required": [
      "type",
      "tags",
      "updated"
    ],
    "conditional_required": {
      "parent_folder": {
        "condition": "path_depth >= 2",
        "condition_semantics": "depth is the number of path segments between the vault root and the file; depth 1 = `<cluster>/_index.md` (parent_folder omitted because the parent IS the vault root); depth >= 2 = `<cluster>/<instance>/_index.md` or deeper (parent_folder mandatory).",
        "rationale": "Navigation-side lineage pointer auto-populated by post-write-verify auto-bootstrap; librarian index-maintain audits for path-vs-frontmatter drift. Distinct from the content-side folder-lineage convention (`engagement:`/`project:`) which uses `_path_rules`. See ../research/vault-construction/_index.md-design.md §parent_folder."
      }
    },
    "optional": [
      "provides",
      "description"
    ],
    "body_structure_contract": "governance/file-type-contracts/_index.md.json",
    "body_structure_contract_rationale": "Body-structure rules (H1, folder-context paragraph, contents-enum table with column shape + sentinel markers + ordering convention) live in the per-file-type contract following the k8s ValidatingAdmissionPolicy paramKind pattern (matcher in pillar/schema; parameters in separate replaceable resource). See ../research/vault-construction/_index.md-design.md + ../target-state/_index.md-design/structural-requirements.md."
  },

  "weekly-summary": {
    "tier": "strict",
    "required": [
      "type",
      "week",
      "date-range",
      "tags"
    ],
    "optional": []
  },

  "daily-archive": {
    "tier": "strict",
    "required": [
      "type",
      "month",
      "date-range",
      "tags"
    ],
    "optional": []
  },

  "navigation": {
    "tier": "standard",
    "required": [
      "type",
      "engagement",
      "updated"
    ],
    "optional": [
      "project",
      "max_lines",
      "tags"
    ]
  },

  "personal-initiative": {
    "tier": "standard",
    "required": [
      "type",
      "name",
      "status",
      "owner",
      "updated",
      "tags"
    ],
    "optional": [
      "github_repo",
      "audience",
      "lines",
      "max_lines",
      "provides",
      "started",
      "completed"
    ]
  },

  "briefing": {
    "tier": "strict",
    "required": [
      "type",
      "date",
      "generated",
      "tags"
    ],
    "optional": [
      "max_lines"
    ]
  },

  "strategic": {
    "tier": "standard",
    "required": [
      "type",
      "engagement",
      "status",
      "updated",
      "tags"
    ],
    "optional": [
      "project",
      "provides",
      "max_lines"
    ]
  },

  "planning": {
    "tier": "standard",
    "required": [
      "type",
      "engagement",
      "updated",
      "tags"
    ],
    "optional": [
      "project",
      "provides",
      "max_lines"
    ]
  },

  "archive": {
    "tier": "strict",
    "required": [
      "type",
      "source-path",
      "archived-date",
      "tags"
    ],
    "optional": [
      "engagement",
      "project"
    ]
  },

  "historical-brief": {
    "tier": "standard",
    "required": [
      "type",
      "updated",
      "marked-historical-by"
    ],
    "optional": [
      "kept-as",
      "merged-into",
      "copied-to",
      "tags"
    ]
  },

  "ideation-brief": {
    "tier": "strict",
    "required": [
      "type",
      "title",
      "created",
      "updated"
    ],
    "optional": [
      "status",
      "tags",
      "backlog_item"
    ]
  },

  "packet": {
    "tier": "strict",
    "_description": "Research context packets at all four altitudes (system / engagement / topic / initiative). System-altitude packets canonical at `claude-stem/research/vault-construction/`; engagement/topic/initiative at user vault. Surfaces 180-day `last_reviewed` cadence (system) / 90-day (topic) / continuous (engagement) / closed-at-plan-close (initiative).",
    "required": [
      "type",
      "altitude",
      "scope",
      "validity_window",
      "source_dependencies",
      "last_reviewed",
      "tags",
      "updated"
    ],
    "optional": [
      "canonical_url",
      "url_stability",
      "provides",
      "max_lines",
      "engagement",
      "project"
    ]
  },

  "archetype-template": {
    "tier": "strict",
    "_description": "Scaffold templates at `onboarding/templates/<archetype>/` consumed by install. Pre-populated frontmatter; R-32 100% conformant. Anti-drift principle: archetype templates ship pre-populated upfront; user-added files inherit defaults via templates.",
    "required": [
      "type",
      "archetype",
      "template_for",
      "tags",
      "updated"
    ],
    "optional": [
      "max_lines",
      "provides",
      "lockstep_pair"
    ]
  }
}
