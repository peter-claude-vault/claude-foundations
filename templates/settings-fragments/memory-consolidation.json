{
  "_comment": "Conditional fragment merged when manifest.hooks.memory_consolidation.enabled is true. Adds claude-mem SessionEnd consolidation. Installer deep-merges this fragment into the SessionEnd hooks array of the user's settings.json.",
  "_merge_target": "hooks.SessionEnd[0].hooks",
  "_manifest_flag": "hooks.memory_consolidation.enabled",
  "entries": [
    {
      "type": "command",
      "command": "~/.claude/hooks/memory-consolidation-check.sh"
    }
  ]
}
