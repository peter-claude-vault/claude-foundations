---
parent_plan: 71-claude-foundations-engine-v2
task: T-11.5
type: test-execution-evidence
status: complete
session: S86
date: 2026-05-02
---

# SP07 T-11.5 — macOS-host voice smoke evidence

**Acceptance summary:** All 3 ACs satisfied. Voice path (B/C/D) and typed path (C, parity probe) both green on Peter's macOS host. AR-8 gate cleared for SP08 T-7 acceptance — this file's mtime serves as the structural marker.

## Run metadata

| Field | Value |
|---|---|
| Run timestamp (UTC) | 2026-05-02T22:31:09Z |
| Session | S86 |
| Host | Peter's macOS host (NOT Lima) |
| Claude Code version | 2.1.126 |
| `/voice` feature version | shipped 2026-03-03; supported on v2.1.69+ (tap mode v2.1.116+) |
| Mic permission | granted (verified by successful pre-flight transcript "Test for t eleven point five") |
| Dogfood root (R-55-safe scratch) | `/tmp/sp07-t-11-5-20260502-181735` |
| `$CLAUDE_HOME` (test scope) | `${DOGFOOD_ROOT}/.claude` |
| Foundation-repo HEAD at run | `4110186` (S85 close `4110186` — `skills/adopt/` MVP scaffolding) |

## /voice probe contract

Voice-capture.sh `probe_voice()` resolves in priority order: `VOICE_PROBE_OVERRIDE` env → `CLAUDE_VOICE_AVAILABLE` env → default-deny.

For T-11.5 smoke, `VOICE_PROBE_OVERRIDE=available` was used to force the voice-mode code path through voice-capture.sh. Real `/voice` invocation occurred out-of-band in the harness chat surface — Peter held Space, dictated transcripts via `/voice`, and the resulting text was piped into voice-capture.sh's stdin via the documented uniform-stdin contract (voice-capture.sh L36-38, L246-256).

This is the production seam contract per voice-capture.sh L17-19: the wrapper does not invoke `/voice` directly; it accepts transcript text from the calling agent (Claude in the harness, who CAN invoke `/voice`) and writes to the deterministic per-section path.

## Voice-path execution per section

For each section, the prompt card was rendered to stderr (matching SKILL.md §Per-Section Pipeline step 1), `/voice` produced a transcript in Peter's chat, the transcript was piped to voice-capture.sh stdin, and the script wrote the transcript to `$TRANSCRIPT_DIR/section-{id}.txt` and emitted the absolute path on stdout (last-line contract).

| Section | Prompt card sha256 | Transcript size | Transcript sha256 | Exit code | Voice mode |
|---|---|---|---|---|---|
| B | `2a0a22cf237c64c97139d1b9777b0a8d580bcefb34e89133ef61f44c4669b383` | 7924 bytes | `846d98a123fa18493579bee2900d575180dd6eda78386d5739871a10c93cda0e` | 0 | voice |
| C | `c90c07cecbf6ed34769002b1adb4075b26388e92e60565162d3a987b6d4291d7` | 439 bytes | `5824e8569b6564de9ef94123fc41409207124b96aa914951574e6b465cb629ff` | 0 | voice |
| D | `353570fc75402a9a17739413083b2cf5e41658f8d06411f0ebc3ff077df27f3c` | 432 bytes | `4dc67aafb495d0e69ac93f8dc93385e77b6e003babb9697a9da0c96c4c6e8d5f` | 0 | voice |

Per-section content-equality verified: each written transcript is byte-identical to the captured `/voice` output modulo single trailing-newline strip (documented behavior of voice-capture.sh L255 `$(cat)` + L260 `printf '%s'`).

## Typed-path parity probe (Section C)

To confirm voice/typed pipeline mode-agnosticism on host (typed-textarea.sh AC4), Section C's voice transcript was re-injected through typed-textarea.sh via `STDIN_TRANSCRIPT_OVERRIDE`. Output written to a separate dir (`transcripts-typed/`) for byte-comparison.

| Probe | Transcript size | Transcript sha256 | Exit code |
|---|---|---|---|
| Section C — typed-textarea.sh | 439 bytes | `5824e8569b6564de9ef94123fc41409207124b96aa914951574e6b465cb629ff` | 0 |

**Voice ≡ Typed for same input: BYTE-IDENTICAL** (sha256 collision confirmed; `cmp -s` returned 0). Pipeline is mode-agnostic at the transcript-write layer — same transcript text produces same per-section file regardless of capture mode.

## Hard Rule 9 — reference-leak floor

This evidence file contains structural metadata only (paths, sizes, sha256, exit codes). Transcript bodies are NOT logged here. Per voice-capture.sh contract L60-61, transcript text is structural data; it is written to the per-section file and never logged in stderr/audit metadata. The dogfood transcripts at `/tmp/sp07-t-11-5-20260502-181735/.claude/onboarding/transcripts/` contain Peter's real personal context (clients, colleagues, business specifics) and are scoped to the /tmp/ dogfood scratch — they do NOT propagate to plans-dir, foundation-repo, or vault.

## R-55 compliance

| Write target | Path | R-55 status |
|---|---|---|
| Voice transcripts | `/tmp/sp07-t-11-5-20260502-181735/.claude/onboarding/transcripts/section-{b,c,d}.txt` | OUTSIDE `~/.claude/` — G1 N/A |
| Typed transcript probe | `/tmp/sp07-t-11-5-20260502-181735/.claude/onboarding/transcripts-typed/section-c.txt` | OUTSIDE `~/.claude/` — G1 N/A |
| Prompt card scratch | `/tmp/sp07-t-11-5-20260502-181735/prompt-cards/section-{b,c,d}.txt` | OUTSIDE `~/.claude/` — G1 N/A |
| Voice-input scratch | `/tmp/sp07-t-11-5-20260502-181735/section-{b,c,d}-voice-input.txt` | OUTSIDE `~/.claude/` — G1 N/A |
| **This evidence file** | `~/.claude-plans/71-.../07-onboarder-ux/state/T-11.5-evidence.md` | plans-dir scope — R-55 sanctioned |

Zero non-master Plan 71 nonces consumed.

## AC verification

- [x] **Voice path tested on macOS host pre-release** — Sections B/C/D all written via voice path; exit 0; content-identical to `/voice` output; mic permission verified live.
- [x] **`state/T-11.5-evidence.md` written documenting typed + voice path completions** — this file. Plans-dir-scoped; structural metadata only.
- [x] **Both modes green; gates SP08 T-7 acceptance (per AR-8)** — voice (B/C/D) + typed (C parity probe) both exit 0 on macOS host. mtime of this file is the AR-8 gate marker for T-7 start-mtime comparison.

## CFF surfaced

**CFF-S86-1 — `CLAUDE_VOICE_AVAILABLE` env signal not honored by `/voice`.** Subagent research confirmed `/voice` (Anthropic-shipped 2026-03-03) does not document or set `CLAUDE_VOICE_AVAILABLE` for subprocess inheritance. Voice-capture.sh's `probe_voice()` therefore default-denies for every real adopter unless they manually set the env var or use `VOICE_PROBE_OVERRIDE=available`. Disposition: **STANDALONE follow-up** for v2.0.x patch — probe contract needs to either (a) use a different signal (e.g., chat-surface heuristic, harness API once documented), (b) document `VOICE_PROBE_OVERRIDE=available` as the canonical adopter opt-in, or (c) accept that real adopters will silently fall through to typed mode and treat that as an acceptable degraded baseline. Not S86 scope; not v2.0.0-rc1-blocking (T-11.5 evidence honestly closes via override path; production behavior is documented). Track for v2.0.x.
